#include <iostream>
#include <cassert>

int main()
{
    //double* array = new double[3]; // double*
    
    
    //const double* ptr = array;
    //ptr[0] = 7;
    
    //std::cout << "sizeof(array) = " <<sizeof(array) << ", sizeof(ptr)=" << sizeof(ptr) << std::endl;
    //std::cout << "elements in array: " << (sizeof(array)/sizeof(array[0])) << std::endl;
    
    std::cout << "x=" << 2["hello"] << std::endl;
}